No Prompt:

1. mysql -u root -p
2. inserir a senha
3. create database restaurante;
4. use restaurante;
5. create table pratos(id int auto_increment primary key,
   nome varchar(100) not null,
   ingredientes varchar(255) not null,
   preco decimal(10,2) not null);
6. ctrl+z, depois aperte o ENTER (retorna "bye")
7. se estar na pasta aluno digite: cd Documents
8. git clone https://github.com/lucapaparella/RESTAURANTE.git
9. python -m venv RESTAURANTE
10. no VSCODE: add folder to workspace (pasta RESTAURANTE)
11. terminal => new terminal
12. ./Scripts/activate
13. pip install -r requirements.txt
